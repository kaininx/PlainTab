interface:
  display_name: "Zoom Out PlainTab"
  short_description: "Map unfamiliar PlainTab modules before editing"
  default_prompt: "Use $zoom-out to map this PlainTab area."

policy:
  allow_implicit_invocation: true

